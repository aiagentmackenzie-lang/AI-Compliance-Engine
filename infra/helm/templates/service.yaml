apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-compliance-engine.fullname" . }}
  labels:
    {{- include "ai-compliance-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-compliance-engine.selectorLabels" . | nindent 4 }}
